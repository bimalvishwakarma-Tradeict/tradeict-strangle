"""S007-B — VWAP band + RSI directional engine (reproduction for trade review).

This strategy was already declared TESTED-NEGATIVE. It is rebuilt only so every
trade lands in a per-trade CSV the user can eyeball. No new conclusion is drawn.
"""
